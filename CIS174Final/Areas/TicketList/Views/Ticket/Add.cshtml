@model TicketViewModel

<h2>New Task</h2>

<div asp-validation-summary="All" class="text-danger"></div>

<form asp-action="Add" method="post">

    <div class="form-group">
        <label asp-for="Ticket.Name">Name:</label>
        <input type="text" asp-for="Ticket.Name" class="form-control">
    </div>

    <div class="form-group">
        <label asp-for="Ticket.Description">Description:</label>
        <input type="text" asp-for="Ticket.Description" class="form-control">
    </div>

    <div class="form-group">
        <label asp-for="Ticket.SprintId">Sprint:</label>
        <select asp-for="Ticket.SprintId" class="form-control"
                asp-items="@(new SelectList(ViewBag.Sprint, "SprintId", "Number"))">
            <option value=""></option>
        </select>
    </div>
    <div class="form-group">
        <label asp-for="Ticket.pointId">Point Value:</label>
        <input type="text" asp-for="Ticket.pointId" class="form-control">
    </div>
    <div class="form-group">
        <label asp-for="Ticket.StatusId">Status:</label>
        <select asp-for="Ticket.StatusId" class="form-control"
                asp-items="@(new SelectList(ViewBag.Statuses, "StatusId", "Name"))">
            <option value=""></option>
        </select>
    </div>

    <button type="submit" class="btn btn-primary">Add</button>
    <a asp-action="Index" class="btn btn-primary">Cancel</a>
</form>
